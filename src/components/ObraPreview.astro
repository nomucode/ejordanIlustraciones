---
// src/components/ObraPreview.astro
const { titulo, imagenSrc, slug } = Astro.props;
---

<div class="group relative overflow-hidden rounded-2xl shadow-xl">
  <a href={`/obras/${slug}`} class="block w-full h-full">
    <img
      src={imagenSrc}
      alt={`Ilustración llamada ${titulo}`}
      class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
    />

    <div
      class="absolute inset-0 bg-black bg-opacity-40 flex items-center justify-center p-4
             opacity-0 group-hover:opacity-70 transition-opacity duration-500"
    >
      <h2 class="text-white text-center text-2xl font-semibold break-words">
        {titulo}
      </h2>
    </div>
  </a>
</div>
