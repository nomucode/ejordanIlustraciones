---
// Este componente no necesita props, es autocontenido.
---

<form
  action="https://formspree.io/f/xqadveyw"
  method="POST"
  class="max-w-xl mx-auto text-left space-y-8"
>
  <div>
    {/* ✅ LABEL ESTILIZADO: Mayúsculas y con el color de acento */}
    <label
      for="name"
      class="block text-sm font-bold uppercase tracking-wide text-accent mb-1"
      >Nombre</label
    >
    <input
      type="text"
      name="name"
      id="name"
      required
      class="w-full bg-transparent border-b-2 border-primary/40 px-1 py-2 focus:outline-none focus:ring-0 focus:border-primary transition-colors"
      placeholder="Tu nombre completo"
    />
  </div>

  <div>
    <label
      for="email"
      class="block text-sm font-bold uppercase tracking-wide text-accent mb-1"
      >Email</label
    >
    <input
      type="email"
      name="email"
      id="email"
      required
      class="w-full bg-transparent border-b-2 border-primary/40 px-1 py-2 focus:outline-none focus:ring-0 focus:border-primary transition-colors"
      placeholder="tu@email.com"
    />
  </div>

  <div>
    <label
      for="message"
      class="block text-sm font-bold uppercase tracking-wide text-accent mb-1"
      >Mensaje</label
    >
    <textarea
      name="message"
      id="message"
      required
      rows="5"
      class="w-full bg-transparent border-b-2 border-primary/40 px-1 py-2 focus:outline-none focus:ring-0 focus:border-primary transition-colors"
      placeholder="Hola, me gustaría hablar sobre..."></textarea>
  </div>

  <div class="text-center pt-4">
    {/* ✅ BOTÓN CON MICRO-INTERACCIÓN AÑADIDA */}
    <button
      type="submit"
      class="inline-block px-10 py-3 bg-primary text-white font-bold rounded-full shadow-lg hover:bg-transparent hover:text-primary border-2 border-primary hover:scale-105 transition-all duration-300"
    >
      Enviar Mensaje
    </button>
  </div>
</form>
