---
// src/pages/sobre-mi.astro
import Layout from "../layouts/Layout.astro";
import { getPageInfo } from "../lib/wp";
const { title, content } = await getPageInfo("sobre-mi");
---

<Layout>
  <div class="max-w-5xl mx-auto p-4 md:p-8">
    <div class="md:flex md:gap-12 md:items-start">
      <div class="md:w-1/3 mb-8 md:mb-0">
        <img
          src="https://images.unsplash.com/photo-1583512603805-3cc6b41f3edb?q=80&w=1887"
          alt="Fotografía de la artista Beatriz Estebanez"
          class="rounded-lg shadow-xl w-full h-auto object-cover"
        />
      </div>

      <div class="md:w-2/3">
        <h1 class="text-4xl md:text-5xl font-bold text-zinc-900 mb-6">
          {title}
        </h1>
        <div
          class="text-lg text-zinc-700 leading-relaxed mb-4"
          set:html={content}
        />
      </div>
    </div>
  </div></Layout
>
