---
import Layout from "../layouts/Layout.astro";
import { getPageInfo } from "../lib/wp";

const { title, content, featuredImageUrl } = await getPageInfo("inicio");
---

<Layout>
  <div class="min-h-screen flex flex-col items-center justify-center -mt-24">
    <h1 class="text-4xl md:text-6xl font-bold">{"Triz Jordan"}</h1>
    <div class="text-xl text-zinc-600 mt-2" set:html={content} />
  </div>
</Layout>
